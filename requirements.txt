# Dependencies for the DistilBERT-Stacked Ensemble and BERT-GNN pipelines.
#
# Top-level imports were extracted from both notebooks. Version constraints
# below match the toolchain available at the time of the MSc dissertation
# submission (September 2024) and allow installation in a current Python 3.10+
# environment while staying close to the original setup.
#
# Install (recommended in a clean virtualenv):
#     python -m venv .venv
#     source .venv/bin/activate          # macOS / Linux
#     .venv\Scripts\activate             # Windows
#     pip install -r requirements.txt
#
# Note: torch and torch-geometric should ideally be installed via the PyTorch
# selector at https://pytorch.org/get-started/locally/ to pick the correct
# CUDA/CPU wheel for your hardware. The bounds below are CPU-safe defaults.

# --- Core data / numerics ---
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.5,<2.0
xgboost>=2.0,<3.0
imbalanced-learn>=0.12,<0.15
joblib>=1.4,<2.0
scipy>=1.11,<2.0

# --- Deep learning ---
torch>=2.2,<3.0
torch-geometric>=2.5,<3.0
transformers>=4.40,<5.0

# --- Optimisation ---
optuna>=3.6,<5.0

# --- NLP utilities ---
nltk>=3.8,<4.0
wordcloud>=1.9,<2.0
sqlparse>=0.4,<0.6

# --- Plotting / visualisation ---
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14
networkx>=3.2,<4.0

# --- Jupyter (only needed if running the notebooks locally) ---
jupyter>=1.0,<2.0
